/* eslint-disable no-unused-vars */
import React from 'react';
import PropTypes from 'prop-types';

function Bank(props) {
  // const { statusText } = props;

  return (
    <div id="Bank" className="Bank">
      <span id="type">Bank</span>
    </div>
  );
}
// Footer.propTypes = {
//   statusText: PropTypes.string,
// };
// Footer.defaultProps = {
//   statusText: '',
// };

export default Bank;
